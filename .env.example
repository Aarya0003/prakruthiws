# Use a strong, secret value before enabling the owner-only request list endpoint.
ADMIN_TOKEN=replace-with-a-long-random-secret

# Optional: use an absolute path outside the application folder in production.
# DB_PATH=/var/lib/prakruthi/prakruthi.db

# Optional: defaults to 3000.
# PORT=3000
